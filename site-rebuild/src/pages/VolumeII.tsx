import React from 'react';
import { uiStrings } from '../constants/ui-strings';

const VolumeII: React.FC = () => (
  <div>
    <h1>{uiStrings.pages.volumeII.title}</h1>
    <p>{uiStrings.pages.volumeII.description}</p>
  </div>
);

export default VolumeII;
