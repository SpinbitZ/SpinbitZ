import React from 'react';
import { uiStrings } from '../constants/ui-strings';

const VolumeI: React.FC = () => (
  <div>
    <h1>{uiStrings.pages.volumeI.title}</h1>
    <p>{uiStrings.pages.volumeI.description}</p>
  </div>
);

export default VolumeI;
